# Запуск примеров

```shell
.\gradlew -q runAppWithAgent -PexampleType=hello
.\gradlew -q runAppWithAgent -PexampleType=counter
.\gradlew -q runAppWithAgent -PexampleType=changeMethod
.\gradlew -q runAppWithAgent -PexampleType=changeMethodAnnotation #для работы аннотации, в агенте и классе Person нужно сделать правки
```
